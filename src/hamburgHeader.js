import React from 'react'


function hamburgHeader() {

    const showSettings = (event) => {
        event.preventDefault();
    }
   
    return (
      <div></div> 
    );
}

export default hamburgHeader
